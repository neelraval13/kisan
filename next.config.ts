// import type { NextConfig } from 'next';

// const nextConfig: NextConfig = {
//   /* ------------------------------------------------------- */
//   /* Needed for Firebase “Web Frameworks → Next.js” adapter   */
//   /* ------------------------------------------------------- */
//   output: 'standalone',                       // bundle server into one folder
//   experimental: { esmExternals: 'loose' },    // avoids some CJS ↔ ESM issues

//   /* ------------------------------------------------------- */
//   /* Skip lint / TS errors during production build           */
//   /* (re-enable later when warnings are fixed)               */
//   /* ------------------------------------------------------- */
//   eslint: {
//     ignoreDuringBuilds: true,
//   },
//   typescript: {
//     ignoreBuildErrors: true,
//   },
// };

// export default nextConfig;

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;